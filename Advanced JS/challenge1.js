// CHALLENGE 1:
// Create a function `SayHello` which will accept a person's name and which will return "Hello, Name".

let sayHello = (name, lastname) =>`Hello ${name} ${lastname}`;
console.log(sayHello("keval","Parikh"));